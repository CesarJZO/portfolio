const baseUrl = import.meta.env.VITE_API_URL;

export const articleUrl = `${baseUrl}/articles`;

export const articleApiUrl = `${baseUrl}/api/articles`;

export const imageUrl = `${baseUrl}/images`;

export const imageApiUrl = `${baseUrl}/api/images`;
